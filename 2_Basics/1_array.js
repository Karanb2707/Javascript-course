//Array

let array1 = [1,2,3,6,7]

console.log(array1);
console.log(array1[2]);

//Shallow copy: Shallow copy of an object is copy whose properties share the same references
//If original array changed every copy will change

//want to watch again